import { ArrowRight, CheckCircle2, Clock3, Plus, ShieldCheck } from 'lucide-react';
import { Link } from 'wouter';
import { useListAssessments } from '@workspace/api-client-react';
import { AppShell, EmptyState, ErrorState, LoadingBlocks, StatusPill } from '@/components/portal-shell';
import { Metric, NextLink } from '@/lib/portal-ui';

export default function UserDashboard() {
  const query = useListAssessments(); const assessments = query.data ?? []; const latest = assessments[0];
  return <AppShell role="user"><div className="animate-rise-in"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono-app text-[10px] uppercase tracking-[.18em] text-[hsl(var(--accent-foreground)/.68)]">Company workspace</p><h1 className="mt-3 font-display text-5xl leading-none tracking-[-.055em] text-[hsl(var(--primary))]">Your CSR picture.</h1><p className="mt-4 text-sm text-[hsl(var(--muted-foreground))]">A clear view of applicability, obligation, and what comes next.</p></div><Link href="/user/assess" className="inline-flex items-center justify-center gap-2 rounded-xl bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))]" data-testid="link-new-assessment"><Plus className="size-4" /> New assessment</Link></div>
    {query.isLoading && <div className="mt-10"><LoadingBlocks /></div>}{query.isError && <div className="mt-10"><ErrorState onRetry={() => query.refetch()} /></div>}
    {!query.isLoading && !query.isError && !latest && <div className="mt-10"><EmptyState title="Your workspace is ready." body="Start with your company details. CivicPath will compare your financial inputs against the CSR thresholds and keep the result here." /><div className="mt-5 text-center"><Link href="/user/assess" className="inline-flex items-center gap-2 rounded-xl bg-[hsl(var(--primary))] px-5 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))]" data-testid="link-empty-start">Begin assessment <ArrowRight className="size-4" /></Link></div></div>}
    {latest && <div className="mt-10 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><div className="space-y-6"><section className="overflow-hidden rounded-[24px] border border-[hsl(var(--border))] bg-[hsl(var(--card))] shadow-[0_10px_28px_hsl(var(--primary)/.05)]"><div className="border-b border-[hsl(var(--border))] px-6 py-5 sm:px-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-mono-app text-[10px] uppercase tracking-[.14em] text-[hsl(var(--muted-foreground))]">Assessment #{String(latest.id).padStart(5, '0')} · {latest.financialYear}</p><h2 className="mt-2 font-display text-3xl tracking-[-.04em]">{latest.companyName}</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{latest.cin}</p></div><StatusPill status={latest.reviewStatus} label={latest.reviewStatus === 'pending' ? 'Awaiting review' : latest.reviewStatus} /></div></div><div className="grid gap-3 px-6 py-6 sm:grid-cols-3 sm:px-8"><Metric label="CSR applicability" value={latest.csrApplicable ? 'Applicable' : 'Not applicable'} tone={latest.csrApplicable ? 'green' : 'plain'} /><Metric label="Indicative obligation" value={latest.indicativeCsrObligation != null ? `₹${latest.indicativeCsrObligation.toFixed(2)} cr` : 'Not calculated'} tone="accent" /><Metric label="Review status" value={latest.reviewStatus === 'pending' ? 'In review' : latest.reviewStatus} /></div><div className="flex flex-wrap items-center justify-between gap-3 border-t border-[hsl(var(--border))] px-6 py-4 sm:px-8"><span className="flex items-center gap-2 text-xs text-[hsl(var(--muted-foreground))]"><ShieldCheck className="size-4 text-[hsl(155_42%_43%)]" /> Server-decided, independently reviewed</span><NextLink href={`/user/assessment/${latest.id}`} testId="link-view-latest">View assessment</NextLink></div></section><section className="rounded-[24px] border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 sm:p-8"><div className="flex items-center justify-between"><div><p className="font-mono-app text-[10px] uppercase tracking-[.14em] text-[hsl(var(--muted-foreground))]">Recent assessments</p><h2 className="mt-2 font-display text-2xl">Your record</h2></div><Link href="/user/assess" className="text-xs font-semibold text-[hsl(var(--primary))]" data-testid="link-record-new">New assessment</Link></div><div className="mt-5 divide-y divide-[hsl(var(--border))]">{assessments.slice(0, 5).map((item) => <Link href={`/user/assessment/${item.id}`} key={item.id} className="flex items-center justify-between gap-4 py-4 first:pt-0 last:pb-0" data-testid={`row-assessment-${item.id}`}><div><p className="text-sm font-semibold">{item.companyName}</p><p className="mt-1 font-mono-app text-[10px] text-[hsl(var(--muted-foreground))]">{item.financialYear} · updated {new Date(item.updatedAt).toLocaleDateString('en-IN')}</p></div><div className="flex items-center gap-3"><StatusPill status={item.csrApplicable ? 'applicable' : 'not_applicable'} label={item.csrApplicable ? 'Applicable' : 'Not applicable'} /><ArrowRight className="size-4 text-[hsl(var(--muted-foreground))]" /></div></Link>)}</div></section></div><aside className="space-y-6"><div className="rounded-[24px] bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))] sm:p-7"><p className="font-mono-app text-[10px] uppercase tracking-[.15em] text-[hsl(var(--primary-foreground)/.55)]">Roadmap signal</p><div className="mt-7 space-y-6"><div className="flex gap-3"><CheckCircle2 className="mt-0.5 size-5 text-[hsl(var(--accent))]" /><div><p className="text-sm font-semibold">Financial assessment recorded</p><p className="mt-1 text-xs leading-5 text-[hsl(var(--primary-foreground)/.6)]">Your latest numbers are on file.</p></div></div><div className="flex gap-3"><Clock3 className="mt-0.5 size-5 text-[hsl(var(--accent))]" /><div><p className="text-sm font-semibold">{latest.reviewStatus === 'pending' ? 'Independent review is next' : 'Review status is up to date'}</p><p className="mt-1 text-xs leading-5 text-[hsl(var(--primary-foreground)/.6)]">Open the roadmap for practical readiness steps.</p></div></div></div><Link href="/user/roadmap" className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-[hsl(var(--accent))]" data-testid="link-dashboard-roadmap">Open roadmap <ArrowRight className="size-4" /></Link></div><div className="rounded-[24px] border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><p className="font-mono-app text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Need a hand?</p><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">Ask a question about your assessment or the review process.</p><Link href="/user/support" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[hsl(var(--primary))]" data-testid="link-dashboard-support">Contact support <ArrowRight className="size-4" /></Link></div></aside></div>}</div></AppShell>;
}